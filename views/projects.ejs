<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project Details</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/navbar', { page: '/solutions/projects' }) %>
    <!-- Navbar -->

    <div class="container mx-auto">
        <div class="hero bg-primary text-primary-content p-6 flex flex-col items-center text-center">
            <h1><%= project.title %></h1>
            <p>Below, you will find detailed information about the project: <%= project.title %></p>
        </div>

        <div class="p-6">
            <img src="<%= project.feature_img_url %>" alt="Project Image" class="w-full max-w-sm mx-auto rounded">
            <h2 class="text-lg font-bold mt-4">Introduction</h2>
            <p><%= project.intro_short %></p>

            <h2 class="text-lg font-bold mt-4">Impact</h2>
            <p><%= project.impact %></p>

            <h2 class="text-lg font-bold mt-4">More Information</h2>
            <a href="<%= project.original_source_url %>" target="_blank" class="btn btn-primary">Visit Source</a>

            <h2 class="text-lg font-bold mt-4">Quote of the Day</h2>
            <blockquote id="quote" class="italic"></blockquote>
            <footer id="author" class="text-right mt-2"></footer>

            <div class="mt-6">
                <a href="#" onclick="history.back(); return false;" class="btn btn-secondary">Go Back</a>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            fetch("https://dummyjson.com/quotes/random")
                .then(response => response.json())
                .then(data => {
                    document.getElementById("quote").textContent = `"${data.quote}"`;
                    document.getElementById("author").textContent = `- ${data.author}`;
                })
                .catch(err => {
                    console.error("Error fetching quote:", err);
                });
        });
    </script>
</body>
</html>
